{
  "output_format": "{title}_{year}.pdf",
  "backup_format": "{original_name}_{timestamp}.pdf",
  "rules": [
    {
      "name": "Standard Format",
      "priority": 1,
      "title_pattern": "^(.+?)\\s*(?:\\(|\\[)?(?:19|20)\\d{2}",
      "year_pattern": "(?:19|20)\\d{2}"
    }
  ]
}